; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[platformio]
default_envs = esp32-s3-devkitc-1
extra_configs =
	secrets.ini
	common.ini

[env:nodemcu-32s]
platform = espressif32@~7.0.1
board = nodemcu-32s
framework = espidf
monitor_speed = 115200
upload_speed = 921600
monitor_filters = esp32_exception_decoder
board_build.flash_size = 4MB
board_build.partitions = partitions4.csv
board_build.cmake_extra_args =
	-DSDKCONFIG_DEFAULTS="sdkconfig.common;sdkconfig.esp32"
build_flags = 
	${secrets.build_flags}
	${common.build_flags}
	-DEPD_PIN_SCK=18
	-DEPD_PIN_SDA=23
	-DEPD_PIN_CS=27
	-DEPD_PIN_DC=26
	-DEPD_PIN_RST=25
	-DEPD_PIN_BUSY=33

[env:esp32-s3-devkitc-1]
platform = espressif32@~7.0.1
board = esp32-s3-devkitc-1
framework = espidf
monitor_speed = 115200
board_build.flash_size = 16MB
board_build.partitions = partitions16.csv
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_build.cmake_extra_args =
	-DSDKCONFIG_DEFAULTS="sdkconfig.common;sdkconfig.s3"
build_flags = 
	${secrets.build_flags}
	${common.build_flags}
	-DEPD_PIN_SCK=12
	-DEPD_PIN_SDA=11
	-DEPD_PIN_CS=10
	-DEPD_PIN_DC=9
	-DEPD_PIN_RST=14
	-DEPD_PIN_BUSY=3
	-DEPD_SPI_HOST=SPI2_HOST
